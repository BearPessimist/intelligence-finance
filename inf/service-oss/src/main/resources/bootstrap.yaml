spring:
  cloud:
    nacos:
      config:
        namespace: 8130
        extension-configs:
          - data-id: spring-cloud.yaml
            group: DEV_GROUP
            refresh: true
          - data-id: aliyun.yaml
            group: DEV_GROUP
            refresh: true
