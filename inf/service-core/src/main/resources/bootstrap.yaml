spring:
  cloud:
    nacos:
      config:
        namespace: 2222
        extension-configs:
          - data-id: mysql-datasource.yaml
            group: DEV_GROUP
            refresh: true

          - data-id: spring-cloud-alibaba.yaml
            group: DEV_GROUP
            refresh: true

          - data-id: redis-datasource.yaml
            group: DEV_GROUP
            refresh: true
          - data-id: mybatis-plus.yaml
            group: DEV_GROUP
            refresh: true
